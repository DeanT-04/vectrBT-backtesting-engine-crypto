[project]
name = "quantlab"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "DeanT-04", email = "DeanT-04@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "ccxt>=4.5.68",
    "numpy>=2.4.6",
    "pandas>=3.0.5",
    "pyarrow>=25.0.0",
    "python-dotenv>=1.2.2",
    "vectorbt>=1.1.0",
]

[build-system]
requires = ["uv_build>=0.11.30,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
